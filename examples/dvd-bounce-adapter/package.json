{
	"name": "dvd-bounce-adapter-example",
	"version": "0.0.1",
	"description": "DVD bounce demo with world adapter - Precomputed render list for benchmarking",
	"type": "module",
	"main": "dist/index.js",
	"bin": {
		"dvd-bounce-adapter": "dist/index.js"
	},
	"scripts": {
		"build": "tsup index.ts --format esm --dts",
		"start": "node dist/index.js",
		"dev": "tsx index.ts"
	},
	"dependencies": {
		"bitecs": "^0.4.0",
		"blecsd": "workspace:*"
	},
	"devDependencies": {
		"tsup": "^8.5.1",
		"tsx": "^4.19.3",
		"typescript": "^5.8.2"
	}
}
