/**
 * Audio integration hooks for games.
 * @module audio
 */

export type {
	AudioAdapter,
	AudioChannelValue,
	AudioManager,
	AudioState,
	PlayOptions,
	SoundTrigger,
} from './hooks';
export { AudioChannel, createAudioManager } from './hooks';
